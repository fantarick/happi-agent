# Development configuration for happi-agent v0.2.
# Paths are resolved relative to this file.
state_dir = ".local/state"
worktree_root = ".local/worktrees"
canonical_repo = "../../machine-audits"
policies_dir = "policies"

# Optional overrides; defaults live under state_dir.
# lock_file = ".local/state/happi-agent.lock"
# kill_switch = ".local/state/KILL_SWITCH"
